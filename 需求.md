任务：使用 AReaL 框架实现一个Tool-Integrated Reasoning智能体的端到端训练



任务背景

本次任务要求在 AReaL (https://github.com/inclusionAI/AReaL) 框架中构建一个智能体，并对其进行强化学习（RL）训练。这个智能体能够在CoT过程中，通过多轮工具调用，解决复杂的数学问题。工具包括但不限于python、计算器等等。你可以选择调用任意开源软件中的工具实现，例如rllm（https://github.com/agentica-project/rllm ）等。

任务目标

您的核心任务是：

实现完整的Agent 工作流：

选择合适的工具，并在AReaL中实现对应的工作流。

确保智能体能够正确调用这些工具，并支持多轮交互。

将所有组件串联起来，构建一个端到端的强化学习训练流程。

成功训练并展示结果：

确保整个训练过程能够顺利运行。

观察训练过程中的奖励或性能指标曲线，以证明训练正在有效进行。

将结果整理为报告，并呈现在开源仓库的Pull Request当中

将TIR作为AReaL的实现样例放到examples/tir文件夹下。

添加examples/tir/README.md展示任务报告，包括代码组成逻辑的简介、如何使用、训练效果如何等等。

模型和数据：

模型选择： 您可以使用任何开源的大语言模型（LLM）作为基础模型，我们推荐使用 1.5B 或 7B 规模的模型。

数据： 本次任务重点考察工程实现能力，因此可以使用包括测试数据在内的任何数据进行训练，以快速验证整个流程的正确性，例如AReaL boba （https://huggingface.co/datasets/inclusionAI/AReaL-boba-Data ）


## 实现的注意点

1. tool call的实现方式需要注意, 你不能在生成完整序列后再call tool:
你可能需要设计一个特殊的token，当作stop token，然后此时检查是否是tool call，然后继续生成

2. 设计尽可能的考虑未来的改动，比如多种tool